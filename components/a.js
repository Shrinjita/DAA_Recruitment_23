import React from 'react';
import ReactDOM from 'react-dom';
import { Header, SubscribeButton, Logo, NewsBox, SearchBar } from './components'; // Assuming your components are in a 'components' directory

ReactDOM.render(
    <Header />,
    document.getElementById('app')
);


